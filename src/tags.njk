---
title: Tags
layout: page
---
<style>
.tags-grid {
  gap: var(--s0);
  display: grid;
  grid-template-columns: repeat(3, 1fr);
}

.tags-grid__item {
  margin-block-start: 0 !important;
}

.tags-grid__link {
  background-color: var(--color-lightish);
  display: block;
  padding: var(--s02);
  text-decoration: none;
  /* Reset tap target increase on anchor tags */
  margin: 0;
  color: inherit;
}

.tags-grid__link:hover {
  background-color: var(--color-light);
}
</style>

<ul class="tags-grid" role="list" style="--text-width: 100%;">
{%- for tag in collections.tagList %}
  <li class="tags-grid__item">
    <a class="tags-grid__link" href="{{ tag | url }}">{{ tag }}</a>
  </li>
{%- endfor %}
</ul>
