<article class="Article space-items-base">
  <header class="mb-lg">{# TODO: no bottom margins #}
    <time class="text-muted">{{ item.date | readableDate }}</time>
    <h1 class="mt-xxs">{{ item.data.title }}</h1>
  </header>
  {{ item.templateContent | safe }}
</article>