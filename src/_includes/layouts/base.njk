<!DOCTYPE html>
<html lang="en-us">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <title>{% if title %}{{ title }} | {% endif %}{{ site.title }}</title>
  <link rel="stylesheet" href="{{ '/css/style.css' | url }}">
  <link rel="stylesheet" href="{{ '/css/utilities.css' | url }}">
</head>
<body class="Page{% if pageClass %} {{ pageClass }}{% endif %}">

  <header class="Header Page__grid">
    <a class="SkipNav hidden-til-focus" href="#main">Skip to main content</a>
    <nav>
      <ul class="list-unstyled">
        <li><a {% if (page.url === "/") or (fakeUrl === "home") %}aria-current="page" {% endif %}href="/">{{ site.title }}</a></li>
        {%- for item in site.navigation %}
          <li><a {% if (page.url === item.url) or (fakeUrl === item.url) %}aria-current="page" {% endif %}href="{{ item.url | url }}">{{ item.name }}</a></li>
        {%- endfor %}
      </ul>
    </nav>
  </header>

  <main class="Page__grid mt-10" id="main">
    {{ content | safe }}
  </main>

  <footer class="SiteFooter Page__grid">
    <p class="border-top" style="padding-top: 64px;">© 2021. ⟢ Thanks for visiting!</p>
  </footer>

</body>
</html>
