<!DOCTYPE html>
<html lang="en-us">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <title>{{ site.title }}</title>
  <link rel="stylesheet" href="{{ '/css/style.css' | url }}">
</head>
<body class="Site">

  <a class="hidden-til-focus" href="#main">Skip to main content</a>

  <div class="space-items-xl">

    <header class="Site-header constrain-and-center">
      <em class="Site-logo"><a href="/">{{ site.title }}</a></em>
      <nav>
        <ul class="list-unstyled mt-0">
          {%- for item in site.navigation %}
          <li><a class="{{ "active" if item.url == page.url }}" href="{{ item.url | url }}">{{ item.name }}</a></li>
          {%- endfor %}
        </ul>
      </nav>
    </header>

    {# <main class="space-items-lg"> #}
    <main{% if pageClass %} class="{{ pageClass }}"{% endif %}>
      {{ content | safe }}
    </main>

    <footer class="constrain-and-center border-top">
      <p class="mt-lg">{# ⟢ Made by Jeremy Robert Jones in 2021.&nbsp;&nbsp; #}⟢ 2021. There will be more here soon.</p>
    </footer>

  </div>

</body>
</html>