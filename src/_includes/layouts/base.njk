<!DOCTYPE html>
<html lang="en-us">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <title>{{ site.title }}</title>
  <link
    href="https://fonts.googleapis.com/css2?family=DM+Sans:ital,wght@0,400;0,700;1,400;1,700&amp;family=Lato:ital,wght@0,400;0,700;1,400;1,700&display=swap"
    rel="stylesheet">
  <link rel="stylesheet" href="{{ '/css/style.css' | url }}">
</head>
<body>

  <a class="hidden-til-focus" href="#main">Skip to main content</a>

  <header>
    <em class="logo"><a href="/">{{ site.title }}</a></em>
    <nav>
      {% set nav = site.navigation %}
      <ul>
        {%- for item in nav %}
        <li class="{{ "active" if item.url == page.url }}"><a href="{{ item.url | url }}">{{ item.name }}</a></li>
        {%- endfor %}
      </ul>
    </nav>
  </header>

  <main>
    {{ content | safe }}
  </main>

  <footer>
    <p>⟢ Made by Jeremy Robert Jones in 2021.&nbsp;&nbsp; ⟢ There will be more here soon.</p>
  </footer>

</body>
</html>